import { DisableShortcutsDirective } from './disable-shortcuts.directive';

describe('DisableShortcutsDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableShortcutsDirective();
    expect(directive).toBeTruthy();
  });
});
